import React from 'react';
import { Button } from '../Button/Button';
import { Input } from '../Input/Input';

export const Form = () => {
  return (
    <form>
      <Input />
      <Input />
      <Button></Button>
    </form>
  );
};
