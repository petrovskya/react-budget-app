import styled from 'styled-components';

export const StyledExpenseList = styled.ul`
  display: grid;
  place-items: center;
  width: 100%;
  height: 200px;
  overflow-y: scroll;
  font-size: 16;
  color: rgba(0, 0, 0, 1);
`;
